{% extends 'base.html.twig' %}

{% block title %}Secteur{% endblock %}

{% block body %}
    <h1>Secteur</h1>
<div class="alignDiv">
    <div class="modalAjout" style="height:70vh;padding: 30vh 0;">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ secteur.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ secteur.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('secteur_index') }}" class="btn">Retour à la liste</a>
    </div>
</div>
    {# <a href="{{ path('secteur_edit', {'id': secteur.id}) }}" class="btn">Modifier</a> #}

{#{{ include('secteur/_delete_form.html.twig') }}#}
{% endblock %}

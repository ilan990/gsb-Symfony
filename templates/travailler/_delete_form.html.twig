<form method="post" action="{{ path('travailler_delete', {'id': travailler.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ travailler.id) }}">
    <button class="btn">Delete</button>
</form>

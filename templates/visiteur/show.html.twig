{% extends 'base.html.twig' %}

{% block title %}Visiteur{% endblock %}

{% block body %}
    <h1>Visiteur</h1>

    </table>
<div class="alignDiv">
    <div class="modalAjout" style="height:70vh;padding: 15vh 0;">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ visiteur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ visiteur.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ visiteur.prenom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ visiteur.adresse }}</td>
            </tr>
            <tr>
                <th>CodePostal</th>
                <td>{{ visiteur.codePostal }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ visiteur.ville }}</td>
            </tr>
            <tr>
                <th>DateEmbauche</th>
                <td>{{ visiteur.dateEmbauche ? visiteur.dateEmbauche|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>CodeSecteur</th>
                <td>{{ visiteur.codeSecteur }}</td>
            </tr>
            <tr>
                <th>LaboCode</th>
                <td>{{ visiteur.laboCode }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('visiteur_index') }}" class="btn">Retour à la liste</a>

    <a href="{{ path('visiteur_edit', {'id': visiteur.id}) }}" class="btn">Modifier</a>
    </div>
</div>

{# {{ include('visiteur/_delete_form.html.twig') }}#}
{% endblock %}
